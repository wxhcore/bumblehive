{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default permissions for the BumbleHive main window",
  "windows": ["main"],
  "permissions": ["core:default"]
}
